{
  "name": "five-day-split-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Five-Day Split Tracker for Komal — mobile-first workout tracker (Vite + React frontend, Express + SQLite backend).",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n server,client -c yellow,cyan \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "start:server": "npm run start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

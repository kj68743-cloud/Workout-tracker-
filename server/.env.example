# Port the API server listens on.
PORT=4000

# Directory where the SQLite database file is stored. Point this at a
# persistent volume/disk when deploying (e.g. Render/Railway/Fly disk).
# Defaults to ./data inside the server folder if unset.
# DATA_DIR=/data
